#include <stdio.h>
#include <pthread.h>

typedef struc {
		Element *arr;
		int front;
		int rear;
} queue;

int main(void) {
		char *data;
		data = '\0';
		printf("Enter data : ");
		scanf("%s", data);

		return 0;
}

