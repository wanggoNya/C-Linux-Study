

strstr()  검색 문자열이 대상 문자열에 존재한다면 시작 위치를 반환

1. 한줄씩 읽으면서 문자열[0] 읽음
2. 찾는 글자 수 count 해서 (반복문으로) 문자열[인덱스] 증가시키면서 모두 같으면 (대문자또한 비교해서) 
	해당 인덱스 읽고 char형이니 대문자로 그 글자를 바꾸어서 put 딴파일하고 그 글자수만큼 ++해서 띄어감(중복방지)
3. 안똑같으면 그냥 그 문자 그대로 put